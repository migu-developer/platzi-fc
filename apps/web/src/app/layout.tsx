// Root layout — delegates to [locale]/layout.tsx for i18n
// This file is required by Next.js but content is minimal
// since the locale layout handles fonts, metadata, and providers.

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
